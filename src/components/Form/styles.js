import { StyleSheet } from 'react-native';

const FromStyles = StyleSheet.create({
  input: {
    backgroundColor: '#fff',
    borderRadius: 5,
    elevation: 2,
    paddingHorizontal: 8,
    marginVertical: 10,
  },
  button: {
    marginTop: 20,
  },
});

export default FromStyles;
